spring:
  application:
    name: task-ms
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/db_task_ms?createDatabaseIfNotExist=true&serverTimezone=America/Sao_Paulo
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081